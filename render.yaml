services:
  - type: web
    name: rerender-clean-studio
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -c "from rembg import new_session; new_session('u2netp')"
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
